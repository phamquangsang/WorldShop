<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="thefour.com.worldshop">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity
            android:name=".activities.SplashActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.LoginActivity" />
        <activity
            android:name=".activities.HomeActivity"
            android:clearTaskOnLaunch="true"
            android:label="@string/title_activity_home" />
        <activity
            android:name=".activities.ShoppingActivity"
            android:label="@string/title_activity_shopping" />
        <activity
            android:name=".activities.TravellingActivity"
            android:label="@string/title_activity_traveling" />
        <activity
            android:name=".activities.SelectTravelingCityActivity"
            android:label="@string/title_activity_select_travelling"
            android:noHistory="true" />
        <activity
            android:name=".activities.MakeOfferActivity"
            android:label="@string/title_activity_make_offer"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activities.RequestDetailActivity"
            android:label="@string/title_activity_request_detail"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>